# Landing

Открой `landing/index.html` в браузере (или подними простой сервер, если нужно тестировать ссылки/кеширование):

```bash
cd landing
python3 -m http.server 8080
```

Дока плагина лежит в `../documentation/` — на лендинге кнопки ведут туда же.

